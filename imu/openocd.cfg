 source [find interface/cmsis-dap.cfg]
# source [find interface/jlink.cfg]

transport select swd

source [find target/stm32f4x.cfg]

adapter speed 1000

# Zephyr on STM32F4 typically places SRAM at 0x2000_0000.
# RTT control block (_SEGGER_RTT) is expected in RAM section,
# so search in the main SRAM window for reliable attach.
init
reset halt

# 关键：让程序运行到 main（RTT 已初始化）
# 方法：临时 resume 一下，让程序跑起来
resume
sleep 200   ;# 等待 200ms
halt

# 使用精确地址
rtt setup 0x20000410 0x200 "SEGGER RTT"
rtt start
rtt server start 9090 0

echo "RTT server running on port 9090"
